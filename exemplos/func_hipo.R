# função que calcula hipotenusa 

my_hipo <- function( a,b ){ #list the arguments
  hpy <- sqrt( a^2 + b^2 ) # perform the computation
  
  return( hpy ) #return the value
}

my_hipo(3,4) # call function my_hipo, passing parameters

